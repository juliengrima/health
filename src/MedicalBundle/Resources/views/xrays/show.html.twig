{% extends 'base.html.twig' %}

{% block body %}
    <h1>Xray</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ xray.id }}</td>
            </tr>
            <tr>
                <th>Xrays</th>
                <td>{{ xray.xrays }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('xrays_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('xrays_edit', { 'id': xray.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
